import 'package:flutter/material.dart';

class ScanProductPage extends StatelessWidget {
  const ScanProductPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is the scan product page'),
    );
  }
}
